# TensorRT Inference Server ModelConfig Builder

First [pybind11](https://github.com/pybind/pybind11) module in Yais.

Accepts a repeated list of engine files and generates a TensorRT Inference Model Store.
